<h1>Active Boats list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Owner</th>
            <th>City</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in actives %}
        <tr>
            <td>{{ entity.id }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.owner }}</td>
            <td>{{ entity.city }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>


<br />
<h1>Boats grouped by Owner & City</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Id</th>
            <th>City</th>
            <th>Quantity</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in grouped %}
        <tr>
            <td>{{ entity.id }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.id }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.quantity }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>


